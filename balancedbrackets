function isValid(input){
    var x = "()";
    var y = "{}";
    var z = "[]";
for (i = 0, i < input.length/2, i++){
input = input.replace("()");
input = input.replace("{}");
input = input.replace("[]");

}
}
  
